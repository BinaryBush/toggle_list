name: toggle_list_example
description: Easy to use example application for showcasing the strengths of toogle_list package
publish_to: 'none'
version: 0.0.1

environment:
  sdk: '>=2.15.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  toggle_list:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:

  uses-material-design: true
